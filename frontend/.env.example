# Canonical public origin used for metadata and absolute URLs.
NEXT_PUBLIC_SITE_URL=https://leerraum.ch

# Optional deployment environment marker used by supported hosting platforms.
# VERCEL_ENV=preview
