# Discovery route group

This route group will contain search, browse, and curated discovery surfaces.

Raw filters and search states must remain non-indexable. Only editorially approved
collections with substantive introductions and complete entity relationships may be
published as indexable routes.

Do not add placeholder `page.tsx` files before the discovery domain is implemented and
the corpus is large enough to provide useful results.
